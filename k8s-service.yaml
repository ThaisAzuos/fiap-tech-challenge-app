apiVersion: v1
kind: Service
metadata:
  name: oficina-service
spec:
  selector:
    app: oficina-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer # Use ClusterIP se for usar Ingress
