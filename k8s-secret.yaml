apiVersion: v1
kind: Secret
metadata:
  name: oficina-secrets
type: Opaque
stringData:
  # Valores devem ser definidos via variáveis de ambiente ou configmaps
  # NÃO commite valores reais no repositório
  db-password: ${DB_PASSWORD:oficina_password}
  # jwt-secret removido, pois a aplicação não gera mais JWTs
  jwt-public-key: ${JWT_PUBLIC_KEY} # Adicionado para validação JWT
  new-relic-license-key: "9ffb37976c86f1e8fa44890a0cd416db6949NRAL" # Idealmente, também deveria ser um placeholder
