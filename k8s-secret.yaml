apiVersion: v1
kind: Secret
metadata:
  name: oficina-secrets
type: Opaque
stringData:
  # Substituir antes de aplicar em ambientes reais
  db-password: CHANGE_ME_DB_PASSWORD
  jwt-secret: CHANGE_ME_JWT_SECRET
